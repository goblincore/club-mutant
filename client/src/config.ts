/**
 * Set to true to enable debug keyboard shortcuts:
 * - V/B/N/M: shader toggles (VHS, CRT, Waxy, TV static overlay)
 * - 1/2/3/4: animation tests (burn, flamethrower, punch, hit)
 * - 5: cycle ripped animations
 * - T: boombox animation toggle
 */
export const DEBUG_MODE = false
