---
// src/components/common/WindowMinimize.astro
export interface Props {
  windowId: string;
}

const { windowId } = Astro.props;
---

<div class="min-btn" onclick={`minimizeWindow('${windowId}')`} title="Minimize">
  <img src="/icons/ui/shade-inactive.png" alt="Minimize" />
</div>
